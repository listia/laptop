fancy_echo "Installing rvm, to change Ruby versions ..."
  where rvm | grep 'not found'
  if [ "1" = "$?" ]; then
    echo "Found an rvm"
  else
    curl -L https://get.rvm.io | bash -s stable
    source ~/.rvm/scripts/rvm
  fi

